Flask>=2.0
google-cloud-storage>=2.0.0
# Pinning google-cloud-aiplatform to a specific version to troubleshoot
# an issue with GenerativeModel.from_vertex_endpoint availability.
# Previous: google-cloud-aiplatform>=1.47.0
google-cloud-aiplatform==1.95.0 # For Vertex AI SDK, including RAG
gunicorn>=20.0.0 # For serving the Flask app in Cloud Run
